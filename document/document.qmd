---
title: Climate Impact Analysis of Bat Lake Salamanders (*A. maculatum*)
abstract: This report describes an analytical pipeline forecasting a northern population of yellow spotted salamanders over the remainder of the 21st century. The analysis exists as part of a larger research paper (Kaufman et al., 20XX), and much of the text, figures, and tables in this report were taken directly from there. All code and data used to complete this sub-analysis are available  at <https://github.com/lepasserin/a-maculatum-climate-impact>.
author: Benedict Cummins-Mburu
date: today
date-format: long
format: pdf
linestretch: 1.5
number-sections: true
bibliography: references.bib
---

<!-- Questions for Dylan:
        - Just for fun, do you have an R^2 value for the fitted Bayesian model? How much variance is explained by just the two climate variables?
-->

<!-- Chat with Doug:
        - I have altered it to de-emphasize the population model. 
        - Model is a matrix for the structural incorporation of lag, and no other reason. Lag is obviously important in a long-lived species.
        - Literally every assumption about the matrix has no effect on the mean trend, excpet the lambda = 1. Should I argue that (1) we set it to zero becuase no discernable trend over BLISS period, (2) that's the only reasonable value we could put it at, and (3) represents the "treeatment vs. control" case. Shows how Dylan's discovered effects affect survuvorship in the absence of other effects. 
        - The only two other things are:
            - Lack of confidence intervals
            - Extra variance in temperature (more realistic) is expected to decrease population sizes. So estimates are a little optimistic because of that.
        - I disagree that there should be the matrix in my Methods section. It's not the main thing, and lowkey misleading.
-->

```{r}
#| include: false

# get libraries
library(tidyverse)
library(kableExtra)
library(lubridate)
library(itsmr)
library(patchwork)

# get datasets
BC_coefficients <- read_csv(here::here("data/bias_correction_coefficients.csv"))
ECCC_projections_BC <- read_csv(here::here("data/climate_data/clean_data/ECCC_climate_projections_BC.csv"))
AEG_historic <- read_csv(here::here("data/climate_data/clean_data/AEG_historic_climate.csv"))
index_forecasts <- read_csv(here::here("data/forecast_data/index_forecasts.csv"))
BLISS_data <- read_csv(here::here("data/BLISS_data.csv"))
```

# Abstract (from pre-print)

<!-- Staging -->
Temperate regions experience pronounced interannual climatic variability, providing the opportunity to understand the drivers of temporal variation in survival, and to model future impacts of environmental change. Temperate amphibians are sensitive to climatic variation and are experiencing dramatic declines in range size and abundance under global environmental change. We developed a priori hypotheses for climate effects on amphibian survival, then combine fifteen years of mark-recapture data from a population of spotted salamanders (*Ambystoma maculatum*) with Bayesian hierarchical modelling to test the association between environmental variables and annual survival. We found that greater temperatures during the spring inbound breeding migration led to a 50% decline in annual survival, while greater precipitation during the outbound migration was associated with a survival increase of 30%. Evaporative water loss may be the major driver of survival variation during the spring migration.

<!-- My Section; TODO: Finish writing  this second section of the Abstract when figured out how to reword Results -->
We then projected estimates of adult female survival to the year 2100 under three climate change scenarios while incorporating the estimated effects of environmental variables on adult survival and assuming that assuming that other population parameters would be unchanged. In the milest scenario, the projected increase in spring precipitation were able to compensate for their warmer temperatures, resulting in an approximately stable population size. Faced with more extreme future climates, the population declines to over half of its initial size. Our study draws urgent attention to the migratory dynamics of temperate, spring breeding amphibians faced with a changing climate.

# Methods: Projecting Survivorship & Abundance under Climate Change (from pre-print)

<!-- Overview -->
We paired results from the posthoc breeding migration model with climate forecasting data to project estimates of annual survivorship for the Bat Lake Spotted Salamander population through the remainder of the $\mathrm{21}^\mathrm{st}$ century. First, we forecast the inbound temperature and outbound precipitation covariates from the breeding migration model using climate data from global circulation models (GCMs), under three climate scenarios. These forecasts were then used to estimate future adult female annual survival probabilities. These survivorship estimates were also embedded into a matrix model to project the potential impacts of these changing climatic indices on abundance, in the absence of other effects.

<!-- Downloading climate data -->
Estimates of mean temperature, minimum temperature and total precipitation were required for each day between March–May for the years 2005–2100 to calculate the migration model’s environmental covariates. These data were sourced from the Coupled Model Intercomparison Project Phase 6 (CMIP6) dataset, which provides daily weather predictions that have been statistically downscaled to spatial resolutions of 10 $\text{km}^2$ [@eyring_CMIP6_2016]. We used the closest available grid point to the Algonquin East Gate Climate Station (45° 32' N, 78° 17' W), approximately 2.2 $\text{km}$ away. For each climate variable, the ensemble mean of the 26 available GCMs was used. Three such datasets were compiled, one for each of three Shared Socio-economic Pathways (SSPs) representing conservative (SSP1), moderate (SSP2) and extreme (SSP5) fossil-fueled development respectively [@riahi_scenarios_2017].

<!-- Correcting climate data -->
We applied two statistical bias corrections to the GCM data, in accordance with standard practice for climate impact analysis
[@sharma_BC_2007; @hagemann_BC_2011]. For mean and minimum temperature, additive linear scaling was used to correct for long-run bias in mean and minimum temperature data, following the procedure described in @hempel_tas_2013. For total precipitation, local intensity scaling was used to correct the intensity and frequency of rainy days, applied as described in @smitha_pr_2018. These procedures were applied to the GCM data over a 20-year period (2005-2024) where their results could be directly compared to the Algonquin East Gate Climate Station’s historical records. Once suitable correction factors had been identified, these corrections were applied to future GCM data (see [Appendix -@sec-appendix-bc] for explicit correction equations and a visual comparison of raw versus bias-corrected simulated data).

<!-- Predicting breeding season information -->
Once bias-corrected, GCM data was used to predict the salamander breeding season start and end dates, enabling the calculation of the specific environmental covariates used in the CJS model, for each year through to 2100. For a given year, the breeding season start date was estimated using that year’s mean April temperature (leveraging its strong, negative correlation with Bat Lake ice out date; [Supplement -@sec-appendix-survival]), and was set to end 22 days later, corresponding to the average breeding season length observed during the BLISS study period (2008 to 2022). Estimated intervals were validated by comparing hindcasted and observed environmental indices over this same period ([Supplement -@sec-appendix-survival]). Using these indices, annual survivorship estimates were calculated for each year from 2005 to 2100 using the breeding migration model. This process was repeated for each of the three climate scenarios.

<!-- Forecasting -->
Finally, we used these estimated survival probabilities within a simple matrix model to project long-term population trajectories under different future climate scenarios. We note that this matrix model was not intended to be a realistic population forecast. Our aim was simply to better understand how the environmental effects on survival identified in this study, in isolation, manifest at the population level, in the absence of other climate-driven effects on vital rates, and under the assumption that population growth is currently stable (i.e. $\lambda = 1$).

Our chosen population model is a deterministic, female-only, age-structured matrix model where the final class represents all reproductively mature adults [@smith_leslie_1994]. We chose this model over a simpler one to account for the time lag between changes in adult survivorship and their effects on future adult recruitment. Since female spotted salamanders mature at around 6 years old (Boyle, 2012), this resulted in a seven-staged discrete growth model. We sourced fecundity estimates (i.e. the number of year-old females per adult female alive in the previous year) for the Bat Lake population from @moldowan_spotted_2023. As annual juvenile survivorship from ages 1 to 6 was unknown, these values were set such that the resulting model had a finite rate of increase ($\lambda$) of 1 under baseline conditions [@congdon_snapping_1994]. We considered the baseline conditions to be the average values of the observed environmental indices over the BLISS study period (see [Supplement -@sec-appendix-forecast] for model parameterization). Projections were initialized in the year 2019 at the base model’s stable age distribution with 1840 adult females. We tested the sensitivity of our projections to changes in initial abundance, starting year, and all non-focal vital rates (fecundity, juvenile survival, and age of maturity, holding $\lambda = 1$), finding that such variables had little effect on our results, within reasonable searching ranges [Supplement -@sec-appendix-forecast].

# Results: Projecting Survivorship & Abundance under Climate Change (from pre-print)

<!-- TODO: refactor to first talk about the survivorship trajectories (these should be plotted), and THEN about the resulting demographic predictions. -->



<!-------------------------------------------------------------------------------------------------------------------->

\newpage
\part*{Supplement}
\appendix
\counterwithin{figure}{section}
\counterwithin{table}{section}
\renewcommand{\thefigure}{\thesection\arabic{figure}}
\renewcommand{\thetable}{\thesection\arabic{table}}

# Statistical Bias Corrections {#sec-appendix-bc}

```{r}
#| warning: false
#| message: false
#| echo: false

# get data to be used for diagnostic plotting
AEG_historic_join <- AEG_historic %>%
    select(date, tas, tasmin, pr) %>%
    rename(
        tas_OBS = tas,
        tasmin_OBS = tasmin,
        pr_OBS = pr
    )
BC_diagnostics <- ECCC_projections_BC %>%
    filter(
        scenario == "SSP585",
        year >= 2005,
        year <= 2024
    ) %>%
        left_join(AEG_historic_join, by = "date")

# shared plotting constants for the diagnostic figures below
plot_colours <- c(
    "Observed" = "black",
    "Simulated" = "navyblue",
    "Bias-corrected" = "forestgreen"
)
diagnostic_theme <- theme_classic() +
    theme(
        legend.position = "top",
        legend.title = element_blank(),
        axis.ticks = element_line(colour = "black"),
        axis.text = element_text(colour = "black"),
        axis.title = element_text(size = rel(0.8)),
        plot.title = element_text(face = "bold", size = rel(0.9))
    )

# all panels of the BC diagnostics illustrate the same single year
DIAGNOSTIC_YEAR <- 2010
```

The daily projections of mean temperature, minimum temperature, and total precipitation used in this study (hereafter referred to as "climate projections") were sourced originally from the Coupled Model Intercomparison Project Phase 6 (CMIP6) dataset, which provides daily estimates of these climate variables under various socio-political scenarios (called "Shared Socioeconomic Pathways", often abbreviated as SSPs). Since GCM output is coarse-grained, Environment and Climate Change Canada provides downscaled estimates with higher spatial resolution [10 $\text{km}^2$; @eyring_CMIP6_2016], for each of three SSPs (SSP1, SSP2, and SSP5 ; see [@riahi_scenarios_2017] for detailed definitions). These are more appropriate for the analysis of highly localized populations such as the Bat Lake spotted salamander population. Even with downscaling, however, climate projections are often still systematically biased [@sharma_BC_2007; @hagemann_BC_2011]; while observed trends are often accurately reproduced, estimates may deviate substantially from observations in their absolute values and/or interannual variations.

We implemented two bias correction methods to correct specific, systematic deviations between observed and simulated climate metrics. In both cases, these methods were first applied over a historical period (2005 to 2024) where observed daily climate data was available near our study site (measurements taken from Algonquin East Gate Climate Station), to determine suitable correction factors. Then, these correction factors were applied to correct bias in future projections (through to 2100), under the assumption that systematic bias in the past will manifest itself in the same way in the future. We note here that projections from all three SSPs considered were corrected using the same coefficients. 

## Correcting Temperature Forecasts

For both mean and minimum daily temperature, downscaled estimates showed similar trends to observed measurements over the entire historical period, but with consistently higher monthly averages. Simulated temperatures also exhibited smaller day-to-day variation compared to observed trends, likely because our simulated estimates are based on the ensemble mean of 26 different GCMs, which artificially deflates variance. Methods that correct the variance and the mean in simulated temperature data simultaneously exist, but have been shown to impact the climate change signal in certain datasets [@hagemann_BC_2011]. Given how temperature projections are used in further analyses, we opted to only correct the systematic bias in the average, preserving the climate change signal most faithfully. To this end we applied additive linear scaling following @hempel_tas_2013, implemented as described below.

Let $i$ represent the March of a given year between 2005 and 2024. Then, let $T_{ij}^{\text{OBS}}$ represent the observed temperature (mean or minimum) of the $j^{\text{th}}$ day in that month respectively. $T_{ij}^{\text{SIM}}$ and $T_{ij}^{\text{ADJ}}$ are defined similarly, for simulated (SIM) and bias-corrected (ADJ) daily values. We calculate the linear scaling constant $C$ as follows:

$$
    C = \left(\sum_{i=2005}^{2024}{T_i^{OBS} - T_i^{SIM}}\right) / \space 20
$$ {#eq-additive-linear-scaling-C}

where $T_i^{\text{OBS}}$ and $T_i^{\text{SIM}}$ represent the average temperature of the i^{\text{th}} March from 2005 to 2024, for observed and simulated data respectively. $C$ can then be used to scale future daily temperature simulations like so:

$$
    T_{i,j}^{ADJ} = C + T_{i,j}^{SIM}
$$ {#eq-additive-linear-scaling-application}

Notice that $C$ is calculated over the entire period, and not per-year. This allows us to apply the correction shown in @eq-additive-linear-scaling-application to every March in the forecasted simulated data. This procedure can then be repeated for April and May, mean and minimum temperatures separately, resulting in the estimation of 6 linear scaling constants to correct GCM-derived temperature forecasts.

## Correcting Precipitation Forecasts

For daily total precipitation, downscaled estimates had similar monthly and seasonal averages as observed measurements, but grossly underestimated the day-to-day variance in precipitation, effectively predicting a drizzle each day. This resulted in an underestimated number of rainy days, and an underestimated intensity of wet days. Since the precipitation main effect used in our analysis is sensitive to the number of rainy days (see main text), this drizzle bias required correction. To this end we used local intensity scaling following @smitha_pr_2018, described below.

Let $i$ represent the March of a given year between 2005 and 2024. Then, let $P_{ij}^{\text{OBS}}$ represent the observed precipitation (total) on the $j^{\text{th}}$ day in that month respectively. $P_{ij}^{\text{SIM}}$ and $P_{ij}^{\text{ADJ}}$ are defined similarly, for simulated (SIM) and bias-corrected (ADJ) daily values. First, a fixed threshold $t$ is determined such that the average number of wet days with simulated precipitation totals below $t$ is equal to the average number of observed dry days. This constant is used to correct for the number of dry days in the simulated data. Then, we calculate the observed average precipitation among wet days ($D_1$), and the average precipitation among simulated days above $t$ ($D_2$). Their ratio, $D=\frac{D_1}{D_2}$, is used to correct for the expected intensity of wet days in the simulated data. These constants are then applied to the simulated precipitation data as follows:

$$
P_{i,j}^{ADJ} =\begin{cases}0, & \text{if } P_{i,j}^{SIM} < t \\
\left(P_{i,j}^{SIM} - t\right) \times D, & \text{otherwise}\end{cases}
$$ {#eq-local-intensity-scaling-calculation}

Similarly to temperature, three sets of $(D,t)$ pairs were fit, for each month in our dataset. 

## Bias Correction Results & Diagnostics

All fitted correction factors are shown in @tbl-correction-factors. Monthly biases in simulated temperatures were more extreme earlier in the season, and for minimum vs. average daily temperature estimastes. Simulated precipitation days were deemed dry when their total precipitation did not exceed around 2mm, equivalent to a light shower. Simulated wet days saw their total precipitation amount inflated by a factor of 4 to 7. By construction, the biases we targeted for each climate variable were completely removed when values are averaged over the entire period. However, we also found that, for any specific month in the historical data, raw GCM data was more likely to deviate from observed estimates than bias-corrected GCM data.

@fig-BC-diagnostics compares observed, simulated, and bias corrected daily weather estimates for an arbitrarily chosen year in the historical period (2010). This is shown to illustrate how the correction algorithms modify the simulated data; the effects were qualitaitvely similar for any chosen year within the historical period (2005 to 2024). Since all temperature bias correction coefficients are negative, the simulated trend is pulled downwards. For precipitation, days receiving under ~2mm of precipitation are deemed dry, and wet days see their precipitation estimates inflated, creating daily variability that is similar to the observed trend. 

```{r}
#| warning: false
#| message: false
#| echo: false
#| label: tbl-correction-factors
#| tbl-cap: "Fitted correction factors applied to simulated climate projections."

Table_1 <- read_csv(here::here("data/bias_correction_coefficients.csv")) %>%
    mutate(Value = round(Value, 5))
half <- nrow(Table_1) / 2 # first half is temperature, second half is precipitation
kable(
    cbind(Table_1[1:half, ], Table_1[(half + 1):nrow(Table_1), ]),
    col.names = rep(c("Correction Factor", "Value"), 2),
    booktabs = TRUE
) %>%
    kable_styling(font_size = 9)
```

```{r}
#| warning: false
#| message: false
#| echo: false
#| label: fig-BC-diagnostics
#| fig-cap: "Time series of observed (black), simulated (blue), and bias-corrected (green) daily weather estimates from March 1 to May 31, 2010. Panel A displays mean daily temperature, panel B displays minimum daily temperature, and panel C displays total daily precipitation."
#| fig-height: 7
#| fig-width: 6

# prepare data
Fig_data <- BC_diagnostics %>%
    filter(year == DIAGNOSTIC_YEAR)
startDayOrdinal <- min(Fig_data$ordinalDay)
Fig_data <- Fig_data %>%
    mutate(displayDay = ordinalDay - startDayOrdinal)

# panel A: tas
Panel_A <- Fig_data %>% ggplot(aes(x = displayDay)) +
    geom_line(aes(y = tas_OBS, colour = "Observed"), linewidth = 0.25) +
    geom_line(aes(y = tas, colour = "Simulated"), linewidth = 0.4) +
    geom_line(aes(y = tasBC, colour = "Bias-corrected"), linewidth = 0.4) +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    coord_cartesian(ylim = c(-20, 25)) +
    labs(
        title = "A. Daily Mean Temperature Trend in Spring of 2010",
        x = "Time (Days Since March 1, 2010)",
        y = "Mean Daily Temperature (°C)"
    ) +
    diagnostic_theme

# panel B: tasmin
Panel_B <- Fig_data %>% ggplot(aes(x = displayDay)) +
    geom_line(aes(y = tasmin_OBS, colour = "Observed"), linewidth = 0.25) +
    geom_line(aes(y = tasmin, colour = "Simulated"), linewidth = 0.4) +
    geom_line(aes(y = tasminBC, colour = "Bias-corrected"), linewidth = 0.4) +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    coord_cartesian(ylim = c(-20, 25)) +
    labs(
        title = "B. Daily Minimum Temperature Trend in Spring of 2010",
        x = "Time (Days Since March 1, 2010)",
        y = "Minimum Daily Temperature (°C)"
    ) +
    diagnostic_theme

# panel C: pr
Panel_C <- Fig_data %>% ggplot(aes(x = displayDay)) +
    geom_line(aes(y = pr_OBS, colour = "Observed"), linewidth = 0.25) +
    geom_line(aes(y = pr, colour = "Simulated"), linewidth = 0.4) +
    geom_line(aes(y = prBC, colour = "Bias-corrected"), linewidth = 0.4) +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    coord_cartesian(ylim = c(0, 25)) +
    labs(
        title = "C. Daily Precipitation Trend in Spring of 2010",
        x = "Time (Days Since March 1, 2010)",
        y = " Total Daily Precipitation (mm)"
    ) +
    diagnostic_theme

# combine
(Panel_A / Panel_B / Panel_C) +
    plot_layout(widths = c(1.6, 1), guides = "collect") +
    plot_annotation(theme = theme(legend.position = "top"))
```

# Forecasting Environmental Indices & Survival Probabilities {#sec-appendix-survival}

Bias-corrected daily weather forecasts were used to calculate four key measures, for each year from 2005 to 2100. First, average mean daily tempatures in April were used to predict the breeding season start date (during the BLISS study period, these measures were strongly correlated; $|\rho| = 0.5$). Breeding season end date was calculated directly from the start date, by adding the average breeding season length observed during the BLISS study period. Using the forecasted breeding season interval, we calculated the temperature and precipitation indices as they were calculated in the posthoc breeding migration model (see main text for their formulas). This series of predictions and calculations was made for each climate scenario separatetely.

<!-- TODO: replace the rho value with the actual value. -->

@fig-index-forecast displays the resulting time series of estimates for the breeding season start date and both climate indices. These graphs are also overlayed with the observed values during the BLISS study period. Average index hindcasts over this period were very similar to observed averages, for all climate scenarios. However, observed temperature indices displayed more annual variability that hindcasted ones.

```{r}
#| warning: false
#| message: false
#| echo: false
#| label: fig-index-forecast
#| fig-cap: "Time series of observed (black), and forecasted (coloured) indices, from 2005 to 2100. Panels A, B, and C display breeding season start date, temperature, and precipitation indices respectively. The grey shaded areas represent the BLISS study period (2008 to 2022), and the horizontal dashed lines indicate the average observed index values during this period. The climate scenario labelled 'SSP2' is emphasized for visual contrast."
#| fig-height: 7
#| fig-width: 6

plot_colours <- c(
    "Observed" = "black",
    "SSP1" = "forestgreen",
    "SSP2" = "navyblue",
    "SSP5" = "darkred"
)

# prepare data
BLISS_data_relevant <- BLISS_data %>%
    select(year, breedingSeasonStart, tempIndex, precipIndex)
plotting_data <- index_forecasts %>% pivot_wider(
    id_cols = year,
    names_from = scenario,
    values_from = c(breedingSeasonStart, tempIndex, precipIndex, survivorship)
    ) %>% left_join(
        BLISS_data_relevant,
        by = "year"
    ) %>%
    filter(year >= 2005)

# panel A: breedingSeasonStart
panel_A <- plotting_data %>% ggplot(aes(x = year)) +
    annotate(
        "rect",
        xmin = 2008, xmax = 2022,
        ymin = -Inf, ymax = Inf,
        fill = "grey", alpha = 0.3
    ) +
    geom_line(aes(y = mean(plotting_data$breedingSeasonStart, na.rm = TRUE)), linetype = "dashed") +
    geom_line(aes(y = breedingSeasonStart, colour = "Observed")) +
    geom_line(aes(y = breedingSeasonStart_SSP126, colour = "SSP1"), alpha = 0.4) +
    geom_line(aes(y = breedingSeasonStart_SSP245, colour = "SSP2"), linewidth = 0.8) +
    geom_line(aes(y = breedingSeasonStart_SSP585, colour = "SSP5"), alpha = 0.4) +
    theme_classic() +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    labs(
        title = "A. Breeding Season Starting Date Forecast",
        x = "Time (Years)",
        y = "Estimated Breeding Season Start Date (Ordinal)"
    ) + diagnostic_theme

# panel B: tempIndex
panel_B <- plotting_data %>% ggplot(aes(x = year)) +
    annotate(
        "rect",
        xmin = 2008, xmax = 2022,
        ymin = -Inf, ymax = Inf,
        fill = "grey", alpha = 0.3
    ) +
    geom_line(aes(y = mean(plotting_data$tempIndex, na.rm = TRUE)), linetype = "dashed") +
    geom_line(aes(y = tempIndex, colour = "Observed")) +
    geom_line(aes(y = tempIndex_SSP126, colour = "SSP1"), alpha = 0.4) +
    geom_line(aes(y = tempIndex_SSP245, colour = "SSP2"), linewidth = 0.8) +
    geom_line(aes(y = tempIndex_SSP585, colour = "SSP5"), alpha = 0.4) +
    theme_classic() +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    labs(
        title = "B. Temperature Index Forecast",
        x = "Time (Years)",
        y = "Temperature Index"
    ) + diagnostic_theme

# panel C: precipIndex
panel_C <- plotting_data %>% ggplot(aes(x = year)) +
    annotate(
        "rect",
        xmin = 2008, xmax = 2022,
        ymin = -Inf, ymax = Inf,
        fill = "grey", alpha = 0.3
    ) +
    geom_line(aes(y = mean(plotting_data$precipIndex, na.rm = TRUE)), linetype = "dashed") +
    geom_line(aes(y = precipIndex, colour = "Observed")) +
    geom_line(aes(y = precipIndex_SSP126, colour = "SSP1"), alpha = 0.4) +
    geom_line(aes(y = precipIndex_SSP245, colour = "SSP2"), linewidth = 0.8) +
    geom_line(aes(y = precipIndex_SSP585, colour = "SSP5"), alpha = 0.4) +
    theme_classic() +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    labs(
        title = "C. Precipitation Index Forecast",
        x = "Time (Years)",
        y = "Precipitation Index"
    ) + diagnostic_theme

# combine
(panel_A / panel_B / panel_C) +
    plot_layout(widths = c(1.6, 1), guides = "collect") +
    plot_annotation(theme = theme(legend.position = "top"))
```

Forecasted climate indices (panel B and C in @fig-index-forecast) were used to estimate adult female survivial probabilities over the century, using the posthoc breeding migration model fit in the main text. The survival probability at year $t$ ($S_a(t)$) was derived from the temperature ($\text{Temp}$) and precipitation ($\text{Precip}$) indices estimated for that year using the following formula:

$$
S_a(t) = \text{logit}^{-1}\left[\beta_0 + \beta_1 \text{Temp(t)} + \beta_2 \text{Precip(t)}\right]
$$ {#eq-survival-calculation}
where $\beta_1$, $\beta_2$, and $\beta_3$ were fit using the breeding migration model. The resulting survival forecast for each climate scenario is displayed in @fig-survival-forecast.

```{r}
#| warning: false
#| message: false
#| echo: false
#| label: fig-survival-forecast
#| fig-cap: "Time series of estimated adult female survivorship ($S_a$) from 2005 to 2100, based on environmental data derived from global climate model projections. Three climate scenario (SSP1, SSP2, and SSP3) were considered, and their trends are colored accordingly. The horizontal dashed line indicates the adult female survivorship estimated at the average environmental values observed during the BLISS study period (2008 to 2022). Both smoothed (bold) and raw (faded) projections are shown. Smoothed trends are emphasized to better visualize differences between the climate scenarios."
#| fig-height: 4
#| fig-width: 6

dashed_intercept <- 0.72
MA_RADIUS <- 6

plotting_data <- plotting_data %>%
    mutate(
        survivorship_SSP126_smooth = smooth.ma(survivorship_SSP126, MA_RADIUS),
        survivorship_SSP245_smooth = smooth.ma(survivorship_SSP245, MA_RADIUS),
        survivorship_SSP585_smooth = smooth.ma(survivorship_SSP585, MA_RADIUS)
    )

plot <- plotting_data %>% ggplot(aes(x = year)) +
    geom_line(aes(y = dashed_intercept), linetype = "dashed") +
    geom_line(aes(y = survivorship_SSP126, colour = "SSP1"), alpha = 0.25) +
    geom_line(aes(y = survivorship_SSP245, colour = "SSP2"), alpha = 0.25) +
    geom_line(aes(y = survivorship_SSP585, colour = "SSP5"), alpha = 0.25) +
    geom_line(aes(y = survivorship_SSP126_smooth, colour = "SSP1")) +
    geom_line(aes(y = survivorship_SSP245_smooth, colour = "SSP2")) +
    geom_line(aes(y = survivorship_SSP585_smooth, colour = "SSP5")) +
    theme_classic() +
    scale_colour_manual(
        name = NULL,
        values = plot_colours
    ) +
    labs(
        x = "Time (Years)",
        y = "Estimated Adult Female Survivorship"
    ) + diagnostic_theme

plot
```

# Population Growth Model Specification {#sec-appendix-forecast}

We used a simple, deterministic matrix model to forecast the female Bat Lake spotted salamander population through time. Let $\textbf{N}_t$ be a vector representing the population of salamanders, binned into age groups, in year $t$. Then, the population in the following year ($\textbf{N}_{t+1}$) is calculated as follows:

$$
\textbf{N}_{t+1} = \textbf{N}_t \cdot \textbf{L}_t
$$ {#eq-growth-model}


where $\textbf{L}_t$ is the population projection matrix in year $t$, defined to be:

$$
\textbf{L}_t =
\begin{bmatrix}
0 & 0 & 0 & 0 & 0 & 0 & f \\
S_m & 0 & 0 & 0 & 0 & 0 & 0 \\
0 & S_m & 0 & 0 & 0 & 0 & 0 \\
0 & 0 & S_m & 0 & 0 & 0 & 0 \\
0 & 0 & 0 & S_m & 0 & 0 & 0 \\
0 & 0 & 0 & 0 & S_m & 0 & 0 \\
0 & 0 & 0 & 0 & 0 & S_m & S_a(t)
\end{bmatrix}
$$ {#eq-projection-matrix}

Population growth in this model is governed primarily by the annual adult female survivorship term $S_a\left(t\right)$, which varies from year to year according to those environmental characteristics which correlated with survivorship in the CJS survivorship model fit in the main paper (@eq-survival-calculation). However, population growth in this model also depends on two other life-history parameters ($f$ and $S_m$), which were assumed to be constant over time. $f$ represents the average number of female offspring that will survive a full year that are produced by a single mature female. This was set to 0.705, derived from Moldowan, 2023. $S_m$ represents subadult survivorship, and an estimate for this value could not be found in the literature. As such, $S_m$ was numerically optimized such that the resulting model had a finite rate of increase $\lambda$ near 1, when $S_a\left(t\right)=\text{logit}^{-1}(\beta_0) \approx 0.72$ (i.e., adult female survivorship assuming study-average temperature and precipitation indices). We thought this decision sensible since the population did not show signs of increase or decrease during the BLISS study period (2008 to 2022). This baseline survivorship was also used to derive the stable-age distribution of the population, using the `eigen.analysis` function from the `popbio` package in R. Projections were initialized in the year 2019 at the base model’s stable age distribution with 1840 starting adult females.

Given the uncertainty around some of our modelling decisions, we tested the robustness of our population projections by varying the following elements of our projection and comparing the output with the base run:

  - Different combinations of the non-focal parameters $p$ and $S_m$ (holding $\lambda \approx 1$ in each case)
  - Different age-at-maturity (checked from 5 to 9)
  - Earlier starting year (checked from 2000 to 2025).
  - Different starting abundance (checked from 1000 to 2000).

These tests revealed that our population projection results were similar under other reasonable life-history assumptions and projection initializations. In particular, the relative trends in abundance across different climate scenarios were qualitatively preserved in each case. Therefore, the only effective assumptions being made when translating survivorship forecasts to abundance forecasts is that $\lambda \approx 1$, and that the population is at stable-age distribution.

# References





